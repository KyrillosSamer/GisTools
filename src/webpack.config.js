const path = require('path');

module.exports = {
    // إعداداتك الحالية...
    resolve: {
        fallback: {
            assert: require.resolve('assert/')
        }
    },
    // إعدادات أخرى...
};
